/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['127.0.0.1', 'localhost','lh3.googleusercontent.com'], // tambahkan host API / storage kamu
  },
};

export default nextConfig;
